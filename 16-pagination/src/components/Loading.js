import React from "react";

const Loading = () => {
	const css = {
		margin: "5rem auto",
		textAlign: "center",
		fontSize: "1.5rem",
		color: "#102a42",
	};

	return <div style={css}>Loading...</div>;
};

export default Loading;
