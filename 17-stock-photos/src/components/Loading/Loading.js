import React from "react";

const Loading = () => {
	const css = {
		width: "100%",
		textAlign: "center",
		fontSize: "2rem",
		color: "#333",
	};
	return <div style={{ ...css }}>Loading..</div>;
};

export default Loading;
